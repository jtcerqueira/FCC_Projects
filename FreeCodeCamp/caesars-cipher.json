{"index.js":"/*Program: ceaser-cypher.json\n---------------------------------\nThis program implements ceaser cypher by shifting the letters 13 times clock-wise direction.\nYou can find this code @https://repl.it/@jtcerqueira/Ceaser-Cypher?language=javascript\n@author Júlio Thobias C. Silva*/\n\nfunction rot13(str) { // SERR PBQR PNZC\n\n  // Empty string is done to apend the result.\n  var solution = '';\n\n  // Loops through the letters and return ASCII\n  for(var i = 0; i < str.length; i++){\n    var asciiVal = str[i].charCodeAt();//Give me ASCII number\n\n    /* Return the letter according to ASCII provided they are capital letters.*/\n    if(asciiVal >= 65 && asciiVal <= 77){\n      \n      solution += String.fromCharCode(asciiVal + 13); //Gives me ASCII letter\n\n    // Prevents going over ASCII 90.  \n    } else if(asciiVal >= 78 && asciiVal <= 90){\n    \n      solution += String.fromCharCode(asciiVal - 13);\n\n    // In case the character is not a letter   \n    } else {\n      solution += str[i];\n    }\n\n  }\n\n  return solution;\n\n}\n\n\nrot13(\"LBH QVQ VG!\");"}